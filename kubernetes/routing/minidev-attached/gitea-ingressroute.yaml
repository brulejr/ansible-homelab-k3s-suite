apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: gitea
  namespace: apps
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ gitea_hostname }}`)
      kind: Rule
      services:
        - name: gitea
          port: 3000
  tls: {}
