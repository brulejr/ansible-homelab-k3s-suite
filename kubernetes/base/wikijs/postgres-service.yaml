apiVersion: v1
kind: Service
metadata:
  name: wikijs-postgres
  namespace: apps
spec:
  selector:
    app.kubernetes.io/name: wikijs-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
