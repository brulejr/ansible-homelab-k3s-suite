apiVersion: v1
kind: Service
metadata:
  name: apps-postgres
  namespace: apps
spec:
  selector:
    app.kubernetes.io/name: apps-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
