---
- name: Cluster applications
  hosts: k3s_servers
  become: true
  tasks:
    - name: Include cluster application roles
      ansible.builtin.include_role:
        name: "{{ item }}"
      loop: "{{ cluster_app_roles | default([]) }}"
